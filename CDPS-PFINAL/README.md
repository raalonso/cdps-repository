**Script to deploy a scalable CRM system**

Parameters to invoke script:


To deploy only web servers:

_$ ./deploy_crm.py --nodes s1,s2,s3 deploy_

To deploy all the nodes:

_$ ./deploy_crm.py --nodes all deploy_
